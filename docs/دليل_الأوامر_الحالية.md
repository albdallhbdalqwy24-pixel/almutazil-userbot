# دليل الأوامر الحالية لـ ZTele

> هذا الدليل مولّد من الأوامر المسجلة فعلياً في النسخة الحالية. ابدأ كل أمر بنقطة `.` ومن **نفس حساب Telegram المرتبط بالجلسة**، وليس من البوت المساعد.

## قبل الاستخدام

| الحالة | التوضيح |
|---|---|
| أمر عادي | اكتب الصيغة الظاهرة في عمود الاستخدام. |
| `<النص>` | استبدله بالكلمة أو الرابط أو الاسم المطلوب. |
| `<رقم>` | استبدله برقم صحيح مثل رقم رسالة أو مدة. |
| أمر مجموعة | يحتاج أن تكون داخل مجموعة وقد يحتاج صلاحية مشرف. |
| خدمة خارجية | بعض الأوامر تعتمد على الإنترنت أو خدمة عامة وقد تتأخر أو تفشل من جانب الخدمة. |

## أوامر التشخيص والبداية

| الأمر | الاستخدام |
|---|---|
| حالة التحميل | `.حالة الاوامر` — يعرض الإضافات التي نجح تحميلها أو فشلت. |
| المساعدة | `.مساعدة` أو `.مساعده` — يعرض البداية. |
| الفهرس | `.الاوامر` أو `.الأوامر` أو `.اوامري` — يعرض فهرس الأوامر الأساسي. |
| فحص التشغيل | `.فحص` — يعرض حالة عامة للنسخة. |

## جميع المعالجات المسجلة (548)

قد يظهر نفس الأمر في أكثر من موضع إذا كان له تنفيذ مختلف بحسب السياق. الأوامر الديناميكية أو مراقبات الرسائل مذكورة بوضوح ولا يلزم كتابتها يدوياً.

### إضافة: botcontrols

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `المشتركين$` | `.المشتركين` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `ban_starters` |
| `المحظورين$` | `.المحظورين` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `ban_starters` |
| `مكافح التكرار (تفعيل\|تعطيل)$` | `.مكافح التكرار تفعيل أو تعطيل` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `ban_antiflood` |

### إضافة: iytdl

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `يوت(?:\s\|$)([\s\S]*)` | `.يوت <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `iytdl_inline` |

### إضافة: afk

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `<missing-pattern>` | مراقب تلقائي للأحداث؛ لا يُكتب كأمر يدوي. | هذا handler مراقبة تلقائية وليس أمراً نصياً. — handler: `set_not_afk` |
| `<missing-pattern>` | مراقب تلقائي للأحداث؛ لا يُكتب كأمر يدوي. | هذا handler مراقبة تلقائية وليس أمراً نصياً. — handler: `on_afk` |
| `سليب(?:\s\|$)([\s\S]*)` | `.سليب <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `سليب_ميديا(?:\s\|$)([\s\S]*)` | `.سليب_ميديا <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: android

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `magisk$` | `.magisk` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `kakashi` |
| `device(?: \|$)(\S*)` | `.device <النص>` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `device_info` |
| `codename(?: \|)([\S]*)(?: \|)([\s\S]*)` | `.codename أو <النص> أو <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `codename_info` |
| `twrp(?: \|$)(\S*)` | `.twrp <النص>` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `twrp` |

### إضافة: corecmds

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `(تنصيب\|نصب)$` | `.تنصيب أو نصب` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `install` |
| `حمل ([\s\S]*)` | `.حمل <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `load` |
| `ارسل ([\s\S]*)` | `.ارسل <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `send` |
| `الغاء حمل ([\s\S]*)` | `.الغاء حمل <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `unload` |
| `الغاء نصب ([\s\S]*)` | `.الغاء نصب <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `unload` |
| `الغاء تنصيب ([\s\S]*)` | `.الغاء تنصيب <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `unload` |

### إضافة: json

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `json$` | `.json` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `yaml$` | `.yaml` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |

### إضافة: markdown

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `<missing-pattern>` | مراقب تلقائي للأحداث؛ لا يُكتب كأمر يدوي. | هذا handler مراقبة تلقائية وليس أمراً نصياً. — handler: `reparse` |
| `<missing-pattern>` | مراقب تلقائي للأحداث؛ لا يُكتب كأمر يدوي. | هذا handler مراقبة تلقائية وليس أمراً نصياً. — handler: `mention` |

### إضافة: mention

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `(tagall\|alll)(?:\s\|$)([\s\S]*)` | `.tagall أو alll <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `report$` | `.report` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `men ([\s\S]*)` | `.men <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: tools

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `cur(?:\s\|$)([\s\S]*)` | `.cur <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `currency` |
| `scan( -i)?$` | `.scan( -i)?` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `scan` |
| `decode$` | `.decode` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `parseqr` |
| `barcode ?([\s\S]*)` | `.barcode ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `makeqr(?: \|$)([\s\S]*)` | `.makeqr <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `make_qr` |
| `cal ([\s\S]*)` | `.cal <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `ip(?:\s\|$)([\s\S]*)` | `.ip <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `spy` |
| `ifsc ([\s\S]*)` | `.ifsc <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `color ([\s\S]*)` | `.color <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `xkcd(?:\s\|$)([\s\S]*)` | `.xkcd <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: urltools

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `(دومين\|دنس\|dns)(?:\s\|$)([\s\S]*)` | `.دومين أو دنس أو dns <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `(اختصار\|مصغر)(?:\s\|$)([\s\S]*)` | `.اختصار أو مصغر <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `الغاء اختصار(?:\s\|$)([\s\S]*)` | `.الغاء اختصار <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `اخفاء(?:\s\|$)([\s\S]*)` | `.اخفاء <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `رفع ملف$` | `.رفع ملف` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `upload_files` |

### إضافة: vcmanager

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `بدء مكالمه$` | `.بدء مكالمه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `start_vc` |
| `انهاء مكالمه$` | `.انهاء مكالمه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `end_vc` |
| `دعوه ?(.*)?` | `.دعوه ? <النص>?` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `inv_vc` |
| `معلومات المكالمه` | `.معلومات المكالمه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `info_vc` |
| `عنوان?(.*)?` | `.عنوان? <النص>?` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `title_vc` |
| `(\|الغاء )اسكت ([\s\S]*)` | `.(\|الغاء )اسكت <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `mute_vc` |
| `<missing-pattern>` | مراقب تلقائي للأحداث؛ لا يُكتب كأمر يدوي. | هذا handler مراقبة تلقائية وليس أمراً نصياً. — handler: `unmute_vc` |

### إضافة: vcplayer

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `انضمام ?(\S+)? ?(?:ك)? ?(\S+)?` | `.انضمام ?(S+)? ?ك? ?(S+)?` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `joinVoicechat` |
| `خروج` | `.خروج` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `leaveVoicechat` |
| `قائمة التشغيل` | `.قائمة التشغيل` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `get_playlist` |
| `شغل فيديو ?(1)? ?([\S ]*)?` | `.شغل فيديو ?(1)? ?([S ]*)?` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `play_video` |
| `شغل ?(1)? ?([\S ]*)?` | `.شغل ?(1)? ?([S ]*)?` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `play_audio` |
| `توقف` | `.توقف` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `pause_stream` |
| `كمل` | `.كمل` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `resume_stream` |
| `تخطي` | `.تخطي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `skip_stream` |
| `الميوزك` | `.الميوزك` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `ميوزك` | `.ميوزك` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |

### إضافة: استفتاء

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `استفتاء(?:\s\|$)([\s\S]*)` | `.استفتاء <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `pollcreator` |

### إضافة: افتارات

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `حالات$` | `.حالات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `ستوري انمي$` | `.ستوري انمي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `رقيه$` | `.رقيه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `رمادي$` | `.رمادي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `رماديه$` | `.رماديه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `بيست$` | `.بيست` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `حب$` | `.حب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `رياكشن$` | `.رياكشن` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `ادت$` | `.ادت` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `غنيلي$` | `.غنيلي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `شعر$` | `.شعر` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `ميمز$` | `.ميمز` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `ري اكشن$` | `.ري اكشن` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `معلومه$` | `.معلومه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `تويت$` | `.تويت` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `خيرني$` | `.خيرني` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `ولد انمي$` | `.ولد انمي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `بنت انمي$` | `.بنت انمي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `بنات$` | `.بنات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |

### إضافة: الادمن

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `ح عام(?:\s\|$)([\s\S]*)` | `.ح عام <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zedgban` |
| `الغاء ح عام(?:\s\|$)([\s\S]*)` | `.الغاء ح عام <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zedgban` |
| `العام$` | `.العام` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `gablist` |
| `ط عام(?:\s\|$)([\s\S]*)` | `.ط عام <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `catgkick` |
| `الصورة (وضع\|حذف)$` | `.الصورة وضع أو حذف` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `set_group_photo` |
| `<dynamic:f'{ADMZ}(?:\\s\|$)([\\s\\S]*)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `promote` |
| `رفع مالك(?:\s\|$)([\s\S]*)` | `.رفع مالك <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `promote` |
| `اخفاء(?:\s\|$)([\s\S]*)` | `.اخفاء <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `promote` |
| `<dynamic:f'{UNADMZ}(?:\\s\|$)([\\s\\S]*)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `demote` |
| `<dynamic:f'{BANN}(?:\\s\|$)([\\s\\S]*)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_ban_person` |
| `<dynamic:f'{UNBANN}(?:\\s\|$)([\\s\\S]*)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `nothanos` |
| `<missing-pattern>` | مراقب تلقائي للأحداث؛ لا يُكتب كأمر يدوي. | هذا handler مراقبة تلقائية وليس أمراً نصياً. — handler: `watcher` |
| `<dynamic:f'{KICK}(?:\\s\|$)([\\s\\S]*)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `kick` |
| `تثبيت( بالاشعار\|$)` | `.تثبيت بالاشعار أو` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `pin` |
| `الغاء تثبيت( الكل\|$)` | `.الغاء تثبيت الكل أو` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `unpin` |
| `الاحداث( م)?(?: \|$)(\d*)?` | `.الاحداث( م)? <رقم>?` | استبدل `<رقم>` برقم صحيح عند ظهوره. — handler: `_iundlt` |

### إضافة: الازعاج

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `(تقليد\|ازعاج)$` | `.تقليد أو ازعاج` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `echo` |
| `(الغاء تقليد\|ايقاف التقليد\|ايقاف تقليد\|الغاء الازعاج\|ايقاف الازعاج\|ايقاف ازعاج\|الغاء ازعاج)$` | `.الغاء تقليد أو ايقاف التقليد أو ايقاف تقليد أو الغاء الازعاج أو ايقاف الازعاج أو ايقاف ازعاج أو الغاء ازعاج` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `echo` |
| `حذف المقلدهم( للكل)?` | `.حذف المقلدهم( للكل)?` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `echo` |
| `المقلدهم( للكل)?$` | `.المقلدهم( للكل)?` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `echo` |
| `<missing-pattern>` | مراقب تلقائي للأحداث؛ لا يُكتب كأمر يدوي. | هذا handler مراقبة تلقائية وليس أمراً نصياً. — handler: `samereply` |

### إضافة: الانشاء

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `انشاء (كروب\|خارق\|قناه) ([\s\S]*)` | `.انشاء كروب أو خارق أو قناه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: الاوامر

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `(?:مساعدة\|مساعده)(?: \|$)(.*)` | `.مساعدة أو مساعده <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `basic_help` |
| `(?:اوامري\|الاوامر\|الأوامر)(?: \|$)(.*)` | `.اوامري أو الاوامر أو الأوامر <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `command_index` |
| `حالة (?:الاوامر\|الأوامر)(?: \|$)(.*)` | `.حالة الاوامر أو الأوامر <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `command_load_status` |
| `زدثون` | `.زدثون` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `repozedub` |
| `السورس` | `.السورس` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `repozedub` |
| `سورس` | `.سورس` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `repozedub` |
| `اليدوي(?: \|$)(.*)` | `.اليدوي <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `repozedub` |
| `الفيزا(?: \|$)(.*)` | `.الفيزا <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `repozedub` |
| `الهيروكو(?: \|$)(.*)` | `.الهيروكو <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `repozedub` |
| `التنصيب(?: \|$)(.*)` | `.التنصيب <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `repozedub` |

### إضافة: البروفايل

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `ضع بايو(?: \|$)(.*)` | `.ضع بايو <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `ضع اسم(?: \|$)(.*)` | `.ضع اسم <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `ضع صورة$` | `.ضع صورة` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `(ضع معرف\|ضع يوزر)(?: \|$)(.*)` | `.ضع معرف أو ضع يوزر <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `update_username` |
| `الحساب$` | `.الحساب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `count` |
| `حذف صوره ?(.*)` | `.حذف صوره ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `remove_profilepic` |
| `انشائي$` | `.انشائي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |

### إضافة: التاك

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `all?(.*)` | `.all? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `tagall` |
| `ايقاف التاك?(.*)` | `.ايقاف التاك? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `stop_tagall` |
| `تاك(?:\s\|$)([\s\S]*)` | `.تاك <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `tagall` |
| `تبليغ$` | `.تبليغ` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `منشن(?:\s\|$)([\s\S]*)` | `.منشن <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |

### إضافة: التحشيش

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `رابط الحذف` | `.رابط الحذف` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `بوت الحذف` | `.بوت الحذف` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `رفع جلب(?: \|$)(.*)` | `.رفع جلب <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `رفع مرتي(?: \|$)(.*)` | `.رفع مرتي <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `رفع تاج(?: \|$)(.*)` | `.رفع تاج <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `رفع بكلبي(?: \|$)(.*)` | `.رفع بكلبي <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `رفع بقلبي(?: \|$)(.*)` | `.رفع بقلبي <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `رفع قلبي(?: \|$)(.*)` | `.رفع قلبي <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `رفع جريذي(?: \|$)(.*)` | `.رفع جريذي <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `رفع فرخ(?: \|$)(.*)` | `.رفع فرخ <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `كت(?: \|$)(.*)` | `.كت <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `ahmed` |
| `(نسبه الحب\|نسبة الحب)(?: \|$)(.*)` | `.نسبه الحب أو نسبة الحب <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `(نسبه الانوثة\|نسبة الانوثه\|نسبه الانوثه\|نسبة الانوثة)(?: \|$)(.*)` | `.نسبه الانوثة أو نسبة الانوثه أو نسبه الانوثه أو نسبة الانوثة <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `(نسبه الغباء\|نسبة الغباء)(?: \|$)(.*)` | `.نسبه الغباء أو نسبة الغباء <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `(نسبه الانحراف\|نسبة الانحراف)(?: \|$)(.*)` | `.نسبه الانحراف أو نسبة الانحراف <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `(نسبه المثليه\|نسبة المثليه)(?: \|$)(.*)` | `.نسبه المثليه أو نسبة المثليه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `(نسبه النجاح\|نسبة النجاح)(?: \|$)(.*)` | `.نسبه النجاح أو نسبة النجاح <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `(نسبه الكراهية\|نسبة الكراهيه\|نسبه الكراهيه\|نسبة الكراهية)(?: \|$)(.*)` | `.نسبه الكراهية أو نسبة الكراهيه أو نسبه الكراهيه أو نسبة الكراهية <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `رفع ورع(?: \|$)(.*)` | `.رفع ورع <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع مزه(?: \|$)(.*)` | `.رفع مزه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع مطي(?: \|$)(.*)` | `.رفع مطي <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع حمار(?: \|$)(.*)` | `.رفع حمار <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع خروف(?: \|$)(.*)` | `.رفع خروف <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع حيوان(?: \|$)(.*)` | `.رفع حيوان <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `رفع بزون(?: \|$)(.*)` | `.رفع بزون <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `رفع زباله(?: \|$)(.*)` | `.رفع زباله <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع منشئ(?: \|$)(.*)` | `.رفع منشئ <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع مدير(?: \|$)(.*)` | `.رفع مدير <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع كواد(?: \|$)(.*)` | `.رفع كواد <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع مرتبط(?: \|$)(.*)` | `.رفع مرتبط <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع مرتبطه(?: \|$)(.*)` | `.رفع مرتبطه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع حبيبي(?: \|$)(.*)` | `.رفع حبيبي <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع خطيبتي(?: \|$)(.*)` | `.رفع خطيبتي <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع صاك(?: \|$)(.*)` | `.رفع صاك <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع صاكه(?: \|$)(.*)` | `.رفع صاكه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع حات(?: \|$)(.*)` | `.رفع حات <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رفع حاته(?: \|$)(.*)` | `.رفع حاته <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `اوصف(?: \|$)(.*)` | `.اوصف <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `هينه(?: \|$)(.*)` | `.هينه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `التحشيش` | `.التحشيش` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |

### إضافة: التخزين

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `<missing-pattern>` | مراقب تلقائي للأحداث؛ لا يُكتب كأمر يدوي. | هذا handler مراقبة تلقائية وليس أمراً نصياً. — handler: `monito_p_m_s` |
| `<missing-pattern>` | مراقب تلقائي للأحداث؛ لا يُكتب كأمر يدوي. | هذا handler مراقبة تلقائية وليس أمراً نصياً. — handler: `log_tagged_messages` |
| `خزن(?:\s\|$)([\s\S]*)` | `.خزن <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `log` |
| `تفعيل التخزين$` | `.تفعيل التخزين` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `set_no_log_p_m` |
| `تعطيل التخزين$` | `.تعطيل التخزين` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `set_no_log_p_m` |
| `تخزين الخاص (تفعيل\|تعطيل)$` | `.تخزين الخاص تفعيل أو تعطيل` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `set_pmlog` |
| `تخزين الكروبات (تفعيل\|تعطيل)$` | `.تخزين الكروبات تفعيل أو تعطيل` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `set_grplog` |

### إضافة: التسليه

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `سبونج بوب` | `.سبونج بوب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `kek` |
| `جلبي` | `.جلبي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `dog` |
| `ثعلب` | `.ثعلب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `fox` |
| `فيل` | `.فيل` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `elephant` |
| `معصب` | `.معصب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `homer` |
| `خنزير` | `.خنزير` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `pig` |
| `مان` | `.مان` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `man` |
| `صدمه` | `.صدمه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `sdma` |
| `نادم` | `.نادم` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `nadm` |
| `ترمب` | `.ترمب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `trmb` |
| `همم` | `.همم` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `hmm` |
| `تشان` | `.تشان` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `chan` |
| `صاك` | `.صاك` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `sakk` |
| `معارض` | `.معارض` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `marth` |
| `مروحيه(?: \|$)(.*)` | `.مروحيه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `ابره(?:\s\|$)([\s\S]*)` | `.ابره <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `permalink` |
| `تسليه1` | `.تسليه1` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `افكر$` | `.افكر` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `متت$` | `.متت` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `ضايج$` | `.ضايج` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `ساعه$` | `.ساعه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `مح$` | `.مح` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `قلب$` | `.قلب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `جيم$` | `.جيم` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `<dynamic:f'ارض$'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_` |
| `قمر$` | `.قمر` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `<dynamic:f'اقمار$'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_` |
| `<dynamic:f'قمور$'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_` |
| `القمر$` | `.القمر` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `test` |
| `مدينه$` | `.مدينه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `test` |
| `تسليه2` | `.تسليه2` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `غبي$` | `.غبي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `قتل$` | `.قتل` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `شنو$` | `.شنو` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `طوبه$` | `.طوبه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `مربعات$` | `.مربعات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `حلويات$` | `.حلويات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `حرق$` | `.حرق` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `قنابل$` | `.قنابل` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `جو$` | `.جو` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `كليم$` | `.كليم` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `نار$` | `.نار` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `انتحر$` | `.انتحر` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `مذاكره$` | `.مذاكره` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `تسليه3` | `.تسليه3` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `نجمه$` | `.نجمه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `مكعبات$` | `.مكعبات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `مطر$` | `.مطر` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `تفريغ ?(.*)` | `.تفريغ ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `فليم$` | `.فليم` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `طائره$` | `.طائره` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `شرطه$` | `.شرطه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `النضام الشمسي$` | `.النضام الشمسي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `قاتل( (.*)\|$)` | `.قاتل <النص> أو` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `عين$` | `.عين` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `<dynamic:f'افعى$'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_` |
| `<dynamic:f'رجل$'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_` |
| `فايرس$` | `.فايرس` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `قطار$` | `.قطار` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `<dynamic:f'نيكول$'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_` |
| `<dynamic:f'موسيقى$'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_` |
| `تسليه4` | `.تسليه4` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `رقص$` | `.رقص` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `مستطيل$` | `.مستطيل` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `مربع$` | `.مربع` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `تردد$` | `.تردد` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `دائره$` | `.دائره` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `قلب$` | `.قلب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `قرد$` | `.قرد` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `سرفر$` | `.سرفر` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `يد$` | `.يد` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `تنازلي$` | `.تنازلي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `قلوب$` | `.قلوب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `تسليه5` | `.تسليه5` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `ميت (.*)` | `.ميت <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `kakashi` |
| `مافيا (.*)` | `.مافيا <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `kakashi` |
| `وحش$` | `.وحش` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevilmonster` |
| `خنزير$` | `.خنزير` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevilpig` |
| `مسدس$` | `.مسدس` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevilgun` |
| `كلب$` | `.كلب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevildog` |
| `هلو$` | `.هلو` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevilhello` |
| `طائره$` | `.طائره` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevilhmf` |
| `حبايب$` | `.حبايب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevilcouple` |
| `سوبر$` | `.سوبر` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevilsupreme` |
| `الهند$` | `.الهند` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevilindia` |
| `هلا$` | `.هلا` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevilwelcome` |
| `حيه$` | `.حيه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevilsnake` |
| `باي$` | `.باي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevilbye` |
| `خرا$` | `.خرا` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevilshitos` |
| `دس لايك$` | `.دس لايك` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `bluedevildislike` |
| `تسليه6` | `.تسليه6` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `تهكير$` | `.تهكير` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `احبك` | `.احبك` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `تسليه7` | `.تسليه7` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `نص(?:\s\|$)([\s\S]*)` | `.نص <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `waifu` |
| `ستيكر ?(?:(.*?) ?; )?([\s\S]*)` | `.ستيكر ?(?:(.*?) ?; )? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `sticklet` |
| `هونك(?:\s\|$)([\s\S]*)` | `.هونك <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `honk` |
| `تغريد(?:\s\|$)([\s\S]*)` | `.تغريد <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `twt` |
| `دوغي(?:\s\|$)([\s\S]*)` | `.دوغي <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `doge` |
| `غلاكس(\|ر)(?:\s\|$)([\s\S]*)` | `.غلاكس(\|ر) <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `glax` |
| `تسليه8` | `.تسليه8` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `كرات( (.*)\|$)` | `.كرات <النص> أو` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `🖤( (.*)\|$)` | `.🖤 <النص> أو` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `💙( (.*)\|$)` | `.💙 <النص> أو` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `💜( (.*)\|$)` | `.💜 <النص> أو` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `❤️( (.*)\|$)` | `.❤️ <النص> أو` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `❤️( (.*)\|$)` | `.❤️ <النص> أو` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `❤️( (.*)\|$)` | `.❤️ <النص> أو` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `تسليه9` | `.تسليه9` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |

### إضافة: التفليش

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `اطردني(.*)` | `.اطردني <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `kickme` |
| `مغادره(.*)` | `.مغادره <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `banme` |
| `بوتي$` | `.بوتي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `حالتي ?(.*)` | `.حالتي ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zze` |
| `تفليش بالطرد$` | `.تفليش بالطرد` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `للكل طرد$` | `.للكل طرد` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `<dynamic:f'{TFSH}$'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_` |
| `تصفير$` | `.تصفير` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `(مغادرة الكروبات\|مغادرة المجموعات)` | `.مغادرة الكروبات أو مغادرة المجموعات` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `leave_groups` |
| `مغادرة القنوات` | `.مغادرة القنوات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `leave_channels` |
| `حذف الخاص` | `.حذف الخاص` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `حذف البوتات` | `.حذف البوتات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `تفليش بالبوت$` | `.تفليش بالبوت` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `banavot` |
| `<dynamic:f'{HDRALL}$'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `banavot` |
| `<dynamic:f'{KTMALL}$'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `banavot` |
| `الغاء التفليش` | `.الغاء التفليش` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `unbanbot` |
| `ايقاف التفليش` | `.ايقاف التفليش` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `unbanbot` |

### إضافة: التكرار

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `<dynamic:f'{SPAM} ([\\s\\S]*)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `spammer` |
| `<dynamic:f'{UNSPAM} ?(.*)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `spammer` |
| `كرر ([\s\S]*)` | `.كرر <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `spammer` |
| `تكرار ملصق$` | `.تكرار ملصق` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stickerpack_spam` |
| `وسبام ([\s\S]*)` | `.وسبام <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `tmeme` |
| `سبام ([\s\S]*)` | `.سبام <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `tmeme` |
| `تعبير مكرر$` | `.تعبير مكرر` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `react_spam` |
| `ايقاف التكرار ?(.*)` | `.ايقاف التكرار ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `stopspamrz` |
| `التكرار` | `.التكرار` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |

### إضافة: التنظيف

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `مسح(\s*\| \d+)$` | `.مسح * أو d+` | استبدل `<رقم>` برقم صحيح عند ظهوره. — handler: `delete_it` |
| `.مسح(\s*\| \d+)$` | `..مسح * أو d+` | استبدل `<رقم>` برقم صحيح عند ظهوره. — handler: `delete_it` |
| `بداية الحذف` | `.بداية الحذف` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `purge_from` |
| `نهاية الحذف` | `.نهاية الحذف` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `purge_to` |
| `حذف رسائلي` | `.حذف رسائلي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `purgeme` |
| `تنظيف(?:\s\|$)([\s\S]*)` | `.تنظيف <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `fastpurger` |
| `حذف رسائله( الكل)?(?:\s\|$)([\s\S]*)` | `.حذف رسائله( الكل)? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `fast_purger` |

### إضافة: الثيمات

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `ث1$` | `.ث1` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsTHMAT` |
| `ث2$` | `.ث2` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsTHMAT` |
| `ث3$` | `.ث3` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsTHMAT` |
| `ث4$` | `.ث4` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsTHMAT` |
| `ث5$` | `.ث5` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsTHMAT` |
| `ث6$` | `.ث6` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsTHMAT` |
| `ث7$` | `.ث7` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsTHMAT` |
| `ث8$` | `.ث8` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsTHMAT` |
| `ث9$` | `.ث9` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsTHMAT` |
| `ث10$` | `.ث10` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsTHMAT` |
| `ث11$` | `.ث11` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsTHMAT` |
| `ث12$` | `.ث12` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsTHMAT` |
| `ن1$` | `.ن1` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsfanan` |
| `ن2$` | `.ن2` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsfanan` |
| `ن3$` | `.ن3` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsfanan` |
| `ن4$` | `.ن4` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsfanan` |
| `ن5$` | `.ن5` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsfanan` |
| `ن6$` | `.ن6` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsfanan` |
| `ن7$` | `.ن7` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsfanan` |
| `ن8$` | `.ن8` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsfanan` |
| `ن9$` | `.ن9` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsfanan` |
| `ن10$` | `.ن10` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsfanan` |
| `ن11$` | `.ن11` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsfanan` |
| `ن12$` | `.ن12` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stsfanan` |
| `ثيمات` | `.ثيمات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `zed` |
| `الثيمات` | `.الثيمات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `zed` |

### إضافة: الحاسبة

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `احسب ([\s\S]*)` | `.احسب <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `calculator` |

### إضافة: الردود والترحيب

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `الردود` | `.الردود` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `الترحيب` | `.الترحيب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `<missing-pattern>` | مراقب تلقائي للأحداث؛ لا يُكتب كأمر يدوي. | هذا handler مراقبة تلقائية وليس أمراً نصياً. — handler: `filter_incoming_handler` |
| `رد (.*)` | `.رد <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `add_new_filter` |
| `ردود الكروب$` | `.ردود الكروب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `on_snip_list` |
| `حذف رد ([\s\S]*)` | `.حذف رد <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `remove_a_filter` |
| `حذف الردود$` | `.حذف الردود` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `on_all_snip_delete` |
| `ترحيب(?:\s\|$)([\s\S]*)` | `.ترحيب <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `save_welcome` |
| `حذف الترحيب$` | `.حذف الترحيب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `del_welcome` |
| `الترحيبات$` | `.الترحيبات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `show_welcome` |
| `cleanwelcome (on\|off)$` | `.cleanwelcome on أو off` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `del_welcome` |
| `رحب(?:\s\|$)([\s\S]*)` | `.رحب <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `save_welcome` |
| `حذف رحب$` | `.حذف رحب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `del_welcome` |
| `قائمه رحب$` | `.قائمه رحب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `show_welcome` |

### إضافة: الزخرفه

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `زخرفة ?(.*)` | `.زخرفة ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zilzal` |
| `زغرفه ?(.*)` | `.زغرفه ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zelzal` |

### إضافة: السيرفر

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `(ضع\|جلب\|حذف) الفار ([\s\S]*)` | `.ضع أو جلب أو حذف الفار <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `variable` |
| `(ري\|كلين) لود$` | `.ري أو كلين لود` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `_` |

### إضافة: الصلاة

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `صلاة ([\s\S]*)` | `.صلاة <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `get_adzan` |

### إضافة: الضغط

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `(\|ا)ضغط(?:\s\|$)([\s\S]*)` | `.(\|ا)ضغط <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `ffmpeg_compress` |
| `ffmpegsave(?:\s\|$)([\s\S]*)` | `.ffmpegsave <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `ff_mpeg_trim_cmd` |
| `vtrim(?:\s\|$)([\s\S]*)` | `.vtrim <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `ff_mpeg_trim_cmd` |
| `atrim(?:\s\|$)([\s\S]*)` | `.atrim <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `ff_mpeg_trim_cmd` |
| `ffmpegclear$` | `.ffmpegclear` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `ff_mpeg_trim_cmd` |

### إضافة: الفارات

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `اضف فار (.*)` | `.اضف فار <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `variable` |
| `حذف فار(?:\s\|$)([\s\S]*)` | `.حذف فار <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `variable` |
| `جلب فار(?:\s\|$)([\s\S]*)` | `.جلب فار <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `custom_zed` |
| `وقت(?:\s\|$)([\s\S]*)` | `.وقت <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `variable` |
| `اوامر الفارات` | `.اوامر الفارات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `الفارات` | `.الفارات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `الوقت` | `.الوقت` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |

### إضافة: الفحص

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `<dynamic:f'{STATS}$'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `zed_alive` |

### إضافة: الكاشف

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `اكشف(?: \|$)([\s\S]*)` | `.اكشف <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `الكاشف` | `.الكاشف` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |

### إضافة: الكتابة

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `غامق(?: \|$)(.*)` | `.غامق <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `نسخ(?: \|$)(.*)` | `.نسخ <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `مائل(?: \|$)(.*)` | `.مائل <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: المجموعه

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `stat$` | `.stat` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `stats` |
| `قنواتي (الكل\|ادمن\|مالك)$` | `.قنواتي الكل أو ادمن أو مالك` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `stats` |
| `كروباتي (الكل\|ادمن\|مالك)$` | `.كروباتي الكل أو ادمن أو مالك` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `stats` |
| `تجميع الاعضاء$` | `.تجميع الاعضاء` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `scrapmem` |
| `اضف الاعضاء$` | `.اضف الاعضاء` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `admem` |
| `المشرفين(?:\s\|$)([\s\S]*)` | `.المشرفين <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `الاعضاء(?:\s\|$)([\s\S]*)` | `.الاعضاء <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `get_users` |
| `المعلومات(?:\s\|$)([\s\S]*)` | `.المعلومات <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `info` |
| `اكسباير ?([\s\S]*)` | `.اكسباير ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `رفع الحظر ?(.*)` | `.رفع الحظر ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `مسح المحظورين(.*)` | `.مسح المحظورين <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `المحذوفين ?([\s\S]*)` | `.المحذوفين ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `rm_deletedacc` |
| `احصائياتي$` | `.احصائياتي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `count` |
| `الرابط ?(.*)` | `.الرابط ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `رسائلي$` | `.رسائلي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `zed` |
| `رسائله ?(.*)` | `.رسائله ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `(الرسائل\|رسائل) ?(.*)` | `.الرسائل أو رسائل ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |
| `الحاظرهم$` | `.الحاظرهم` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `main` |
| `قيد (.*)` | `.قيد <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `احذف (.*)` | `.احذف <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: المطور

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `وضع المطور (تفعيل\|تعطيل)$` | `.وضع المطور تفعيل أو تعطيل` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `chat_blacklist` |
| `رفع مطور(?:\s\|$)([\s\S]*)` | `.رفع مطور <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `add_sudo_user` |
| `تنزيل مطور(?:\s\|$)([\s\S]*)` | `.تنزيل مطور <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `المطورين$` | `.المطورين` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `حذف_المطورين` | `.حذف_المطورين` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `حذف المطورين` | `.حذف المطورين` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `مسح المطورين` | `.مسح المطورين` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `تنزيل المطورين` | `.تنزيل المطورين` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `تحكم(s)?(?:\s\|$)([\s\S]*)` | `.تحكم(s)? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `ايقاف تحكم(s)?(?:\s\|$)([\s\S]*)?` | `.ايقاف تحكم(s)? <النص>?` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `التحكم( المعطل)?$` | `.التحكم( المعطل)?` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `المساعد` | `.المساعد` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |

### إضافة: الملصقات

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `ملصق(?:\s\|$)([\s\S]*)` | `.ملصق <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `kang` |
| `حزمه(?:\s\|$)([\s\S]*)` | `.حزمه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `pack_kang` |
| `متحرك$` | `.متحرك` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `pussycat` |
| `حزمة(?:\s\|$)([\s\S]*)` | `.حزمة <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `pic2packcmd` |
| `معلومات الملصق$` | `.معلومات الملصق` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `get_pack_info` |
| `ملصقات ?([\s\S]*)` | `.ملصقات ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `cb_sticker` |
| `حذف_ملصق$` | `.حذف_ملصق` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `zelzal_gif` |

### إضافة: الملكيه

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `تحويل ملكية ([\s\S]*)` | `.تحويل ملكية <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: المنع والتحذير

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `<missing-pattern>` | مراقب تلقائي للأحداث؛ لا يُكتب كأمر يدوي. | هذا handler مراقبة تلقائية وليس أمراً نصياً. — handler: `on_new_message` |
| `منع(?:\s\|$)([\s\S]*)` | `.منع <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `الغاء منع(?:\s\|$)([\s\S]*)` | `.الغاء منع <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `قائمة المنع$` | `.قائمة المنع` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `قائمه المنع$` | `.قائمه المنع` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `تحذير(?:\s\|$)([\s\S]*)` | `.تحذير <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `التحذيرات` | `.التحذيرات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `حذف التحذيرات(?: \|$)(.*)` | `.حذف التحذيرات <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: النظام

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `النظام$` | `.النظام` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `psu` |
| `cpu$` | `.cpu` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cpu` |
| `نظامي$` | `.نظامي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `sysdetails` |
| `الانترنت(?:\s\|$)([\s\S]*)` | `.الانترنت <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `(اعاده تشغيل\|اعادة تشغيل\|اعاده التشغيل\|اعادة التشغيل\|تحديث)$` | `.اعاده تشغيل أو اعادة تشغيل أو اعاده التشغيل أو اعادة التشغيل أو تحديث` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `_` |
| `ايقاف البوت$` | `.ايقاف البوت` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `نوم( [0-9]+)?$` | `.نوم( [0-9]+)?` | استبدل `<رقم>` برقم صحيح عند ظهوره. — handler: `_` |
| `الاشعارات (تفعيل\|تعطيل)$` | `.الاشعارات تفعيل أو تعطيل` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `set_pmlog` |
| `المكاتب$` | `.المكاتب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `librarz` |
| `مكتبة (.*)` | `.مكتبة <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `pipcheck` |
| `فرمته(?: \|$)(.*)` | `.فرمته <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `تاريخ$` | `.تاريخ` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `السرعه$` | `.السرعه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `تاريخ التنصيب$` | `.تاريخ التنصيب` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `zeddd` |
| `الموقع ([\s\S]*)` | `.الموقع <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `gps` |

### إضافة: ايميل وهمي

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `بريد$` | `.بريد` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `zelzal_gpt` |
| `الوارد$` | `.الوارد` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `zelzal_gpt` |

### إضافة: بحث صور

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `صور (.*)` | `.صور <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `خلفيات (.*)` | `.خلفيات <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: بلاغات

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `بلاغ ([\s\S]*)` | `.بلاغ <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `spammer` |
| `<dynamic:f'{UNSPAM} ?(.*)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `spammer` |

### إضافة: بلاي

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `بلاي$` | `.بلاي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `zedrepo` |

### إضافة: تجربة

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `امر التجربه` | `.امر التجربه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `hi` |
| `تجربه(?:\s\|$)([\s\S]*)` | `.تجربه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: تحديث البوت

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `تحديث البوت$` | `.تحديث البوت` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `upstream` |

### إضافة: تحويل الصيغ

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `لصوره$` | `.لصوره` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `لملصق$` | `.لملصق` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |
| `ttf ?(.*)` | `.ttf ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `get` |
| `ftoi$` | `.ftoi` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `on_file_to_photo` |
| `حول ?(.*)` | `.حول ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `spin(?: \|$)((-)?(s)?)$` | `.spin((-)?(s)?)` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `pic_gifcmd` |
| `دائري ?((-)?s)?$` | `.دائري ?((-)?s)?` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `video_catfile` |
| `طباعه (.*)` | `.طباعه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `get` |
| `ftoi$` | `.ftoi` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `on_file_to_photo` |
| `ملصق متحرك(?: \|$)(.*)` | `.ملصق متحرك <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `ممتحركة(?: \|$)((-)?(r\|l\|u\|d\|s\|i)?)$` | `.ممتحركة((-)?r أو l أو u أو d أو s أو i?)` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `pic_gifcmd` |
| `متحرك ?([0-9.]+)?$` | `.متحرك ?([0-9.]+)?` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |

### إضافة: ترجمه صوتيه

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `صوت جوجل(?:\s\|$)([\s\S]*)` | `.صوت جوجل <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: تسمية الميديا

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `rename (.*)` | `.rename <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `إسم (.*)` | `.إسم <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `اسم (.*)` | `.اسم <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: تلكراف

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `(ت(ل)?ك(راف)?) ?(m\|t\|ميديا\|نص)(?:\s\|$)([\s\S]*)` | `.(ت(ل)?ك(راف)?) ?m أو t أو ميديا أو نص <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `ctg(?: \|$)([\s\S]*)` | `.ctg <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `ctg` |

### إضافة: ثيمات_بديل

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `((?:ث\|ن)(?:[1-9]\|1[0-2]))$` | `.(ث أو ن[1-9] أو 1[0-2])` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `local_theme_command` |

### إضافة: جوجل

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `جو ([\s\S]*)` | `.جو <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `gsearch` |
| `مصادر ([\s\S]*)` | `.مصادر <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `مصادر$` | `.مصادر` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `grs` |
| `تحليل(?:\s\|$)([\s\S]*)` | `.تحليل <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `reverse` |
| `جوجل(?:\s\|$)([\s\S]*)` | `.جوجل <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `google_search` |

### إضافة: حساب العمر

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `حساب العمر(?:\s\|$)([\s\S]*)` | `.حساب العمر <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: حفظ الذاتيه

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `الذاتيه` | `.الذاتيه` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `cmd` |
| `<dynamic:f'{POSC}(?: \|$)(.*)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `oho` |
| `(تفعيل الذاتيه\|تفعيل الذاتية)` | `.تفعيل الذاتيه أو تفعيل الذاتية` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `start_datea` |
| `(تعطيل الذاتيه\|تعطيل الذاتية)` | `.تعطيل الذاتيه أو تعطيل الذاتية` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `stop_datea` |
| `اعلان (\d*) ([\s\S]*)` | `.اعلان <رقم> <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `selfdestruct` |
| `إعلان (\d*) ([\s\S]*)` | `.إعلان <رقم> <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `selfdestruct` |

### إضافة: حيوان

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `حيوان(?: \|$)(.*)` | `.حيوان <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `who` |

### إضافة: رساله

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `رساله ?(.*)` | `.رساله ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `writer` |
| `^\:/$` | `.:/` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `kek` |
| `^\-_-$` | `.-_-` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `lol` |
| `^\;_;$` | `.;_;` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `fun` |
| `oof$` | `.oof` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `Oof` |
| `اكتب (.*)` | `.اكتب <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `typewriter` |
| `كرر (\d*) (.*)` | `.كرر <رقم> <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `<dynamic:f'ميمي'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `meme` |
| `<dynamic:f'جف'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `give` |

### إضافة: ريمكسات

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `ريماكس ([\s\S]*)` | `.ريماكس <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `remaxzedthon` |
| `ريمكس ([\s\S]*)` | `.ريمكس <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |

### إضافة: زلزال

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `اغنيه(?: \|$)(.*)` | `.اغنيه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `تطبيق(?: \|$)(.*)` | `.تطبيق <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `رابط(?:\s\|$)([\s\S]*)` | `.رابط <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `linkapk` |
| `فلم ([\s\S]*)` | `.فلم <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `zed` |

### إضافة: سكانر

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `اللغات` | `.اللغات` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `get_ocr_languages` |
| `النص (.*)` | `.النص <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `parse_ocr_space_api` |
| `(\|ا)سكانر(?:\s\|$)([\s\S]*)` | `.(\|ا)سكانر <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `ocr` |
| `اسكانر` | `.اسكانر` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `ocr` |

### إضافة: سكرين

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `(سكرين\|ss) ([\s\S]*)` | `.سكرين أو ss <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `لقطه ([\s\S]*)` | `.لقطه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: عرض

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `عرض$` | `.عرض` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `_` |

### إضافة: فلاتر

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `فرام(ف\|م)?$` | `.فرامف أو م?` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `maccmd` |
| `(حقوق\|اطبع)(?:\s\|$)([\s\S]*)` | `.حقوق أو اطبع <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `memes` |
| `الخطوط(?:\s\|$)([\s\S]*)` | `.الخطوط <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `lang` |
| `خطوط(?:\s\|$)([\s\S]*)` | `.خطوط <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `lang` |
| `رقميه(?:\s\|$)([\s\S]*)` | `.رقميه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `memes` |
| `عكس الالوان$` | `.عكس الالوان` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `memes` |
| `سولار$` | `.سولار` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `memes` |
| `ميرور$` | `.ميرور` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `memes` |
| `قلب الصوره$` | `.قلب الصوره` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `memes` |
| `فلتر رصاصي$` | `.فلتر رصاصي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `memes` |
| `زوم ?([\s\S]*)` | `.زوم ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `memes` |
| `اطار ?([\s\S]*)` | `.اطار ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `memes` |

### إضافة: فلم

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `فلم ([\s\S]*)` | `.فلم <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: فوتوشوب

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `(تحلييل\|تغيير)(?:\s\|$)([\s\S]*)` | `.تحلييل أو تغيير <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `remove_background` |

### إضافة: لوجو

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `(\|s)لوجو(?: \|$)([\s\S]*)` | `.(\|s)لوجو <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `very` |
| `(\|c)lbg(?:\s\|$)([\s\S]*)` | `.(\|c)lbg <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `bad` |
| `lf(\|c\|s\|h\|w\|sc\|sw)(?:\s\|$)([\s\S]*)` | `.lf أو c أو s أو h أو w أو sc أو sw <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `pussy` |
| `(g\|d\|r)lvar(?:\s\|$)([\s\S]*)` | `.g أو d أو rlvar <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `cat` |

### إضافة: لوف

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `لوف ?(.*)` | `.لوف ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: لوقو

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `لوكو ?(.*)` | `.لوكو ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `lg1` |
| `لوقو ?(.*)` | `.لوقو ? <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `lg1` |

### إضافة: مشاهير

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `مشهور(?: \|$)(.*)` | `.مشهور <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `who` |
| `مشهوره(?: \|$)(.*)` | `.مشهوره <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `who` |
| `زاحف(?: \|$)(.*)` | `.زاحف <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `who` |

### إضافة: منع التكرار

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `<missing-pattern>` | مراقب تلقائي للأحداث؛ لا يُكتب كأمر يدوي. | هذا handler مراقبة تلقائية وليس أمراً نصياً. — handler: `_` |
| `ضع التكرار(?:\s\|$)([\s\S]*)` | `.ضع التكرار <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

### إضافة: نرد

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `اكس او$` | `.اكس او` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `gamez` |
| `<dynamic:f'({DART_E_MOJI}\|سهم)( ([1-6])\|$)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_` |
| `<dynamic:f'({DICE_E_MOJI}\|نرد)( ([1-6])\|$)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_` |
| `<dynamic:f'({BALL_E_MOJI}\|سله)( ([1-5])\|$)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_` |
| `<dynamic:f'({FOOT_E_MOJI}\|.كرة)( ([1-5])\|$)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_` |
| `<dynamic:f'({SLOT_E_MOJI}\|حظ)( ([1-64])\|$)'>` | أمر ديناميكي داخلي؛ راجع ملف الإضافة أو `.حالة الاوامر`. | لا يمكن استخراج الاسم حرفياً لأن السورس يبنيه من إعداد داخلي. — handler: `_` |

### إضافة: هيروكو

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `(set\|get\|del) var ([\s\S]*)` | `.set أو get أو del var <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `variable` |
| `اسستخدامي$` | `.اسستخدامي` | اكتب الأمر كما هو من نفس الحساب المرتبط بالجلسة. — handler: `dyno_usage` |
| `(سجل التنصيب\|السجلات)$` | `.سجل التنصيب أو السجلات` | يمكن استعمال أي صيغة ظاهرة بين البدائل. — handler: `_` |

### إضافة: وهمي

| الأمر / النمط | الاستخدام | الملاحظة |
|---|---|---|
| `الطقس(?: \|$)(.*)` | `.الطقس <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `طقس (.*)` | `.طقس <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `وهمي كتابه(?: \|$)(.*)` | `.وهمي كتابه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `وهمي صوت(?: \|$)(.*)` | `.وهمي صوت <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `وهمي فيديو(?: \|$)(.*)` | `.وهمي فيديو <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `وهمي لعبه(?: \|$)(.*)` | `.وهمي لعبه <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `وهمي صوره(?: \|$)(.*)` | `.وهمي صوره <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `وهمي موقع(?: \|$)(.*)` | `.وهمي موقع <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `وهمي جهات(?: \|$)(.*)` | `.وهمي جهات <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `وهمي ملف(?: \|$)(.*)` | `.وهمي ملف <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `وهمي مرئي(?: \|$)(.*)` | `.وهمي مرئي <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |
| `ايقاف الوهمي(?: \|$)(.*)` | `.ايقاف الوهمي <النص>` | استبدل `<النص>` بالقيمة المطلوبة. — handler: `_` |

## أوامر الثيمات

الأوامر المتاحة من أصول المستخدم المرفوعة هي `.ث1` للصورة و`.ث2` للفيديو. أما بقية أوامر الثيمات (`.ث3` إلى `.ث12` و`.ن1` إلى `.ن12`) فستظهر رسالة واضحة إلى أن يُضاف لها محتوى.

## عند عدم استجابة أمر

استخدم `.حالة الاوامر` أولاً. إذا كان الأمر يتطلب الرد على رسالة أو يكون خاصاً بالمجموعات أو يحتاج صلاحية مشرف، نفّذه في السياق الصحيح. عند ظهور رسالة الخطأ الجديدة، انسخ اسم الأمر وآخر سطور سجل Render لتحديد المتطلب الناقص بدقة.
