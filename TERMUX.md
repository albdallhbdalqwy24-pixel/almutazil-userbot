# إعداد AlMutazil محلياً على Termux

يضيف هذا المستودع معالجاً محلياً باسم `termux_setup.py`. لا توجد لوحة عامة ولا رابط خارجي؛ الصفحة تستمع إلى `127.0.0.1` على الهاتف نفسه، وتحتاج رمز رابط عشوائياً جديداً في كل مرة.

> لا ترسل `API_HASH` أو `STRING_SESSION` أو توكن البوت أو رمز تسجيل الدخول إلى أي شخص، ولا ترفع `config.py` إلى GitHub.

## التثبيت

نفّذ الأوامر التالية في Termux بعد تنزيل المستودع:

```bash
pkg update -y && pkg upgrade -y
pkg install -y python git ffmpeg python-numpy python-lxml
cd ~/almutazil-userbot
python -m pip install --upgrade pip setuptools wheel
python termux_setup.py
```

سيفتح Termux المتصفح تلقائياً. إذا لم يفتح، انسخ الرابط الذي يبدأ بـ`http://127.0.0.1:8787/?token=` من شاشة Termux وافتحه في متصفح **الهاتف نفسه**.

## داخل صفحة الإعداد

اكتب `APP_ID` و`API_HASH` من موقع Telegram الرسمي، ثم اضغط **استخراج String Session من Telegram**. في صفحة الاستخراج اكتب رقم الهاتف بالصيغة الدولية، مثل `+9665xxxxxxxx`، ثم أدخل الكود الذي يصلك في Telegram وكلمة مرور التحقق بخطوتين إذا طلبها. عند نجاح التسجيل، ارجع إلى صفحة الإعداد؛ الجلسة ستظهر في خانتها تلقائياً.

أدخل توكن بوت الخدمة في `TG_BOT_TOKEN`. يستطيع الحقلان الخاصان بمجموعتي السجل والتخزين البقاء على `0` إذا لا تريد استخدامهما؛ لن ينشئ إعداد Termux أي مجموعة تلقائياً. إن كانت لديك مجموعة موجودة، ألصق معرفها الكامل بالشكل `-100...` كما هو.

بعد اختيار **حفظ الإعدادات وبدء التثبيت**، ينشئ المعالج ملف `config.py` محلياً بصلاحيات قراءة وكتابة للمالك فقط، ويبدأ التثبيت ثم التشغيل. راقب المخرجات عند الحاجة عبر:

```bash
cd ~/almutazil-userbot
tail -n 80 termux_setup.log
```

## التشغيل في وقت لاحق

بعد اكتمال الإعداد الأول، لا تحتاج فتح الصفحة مرة أخرى. شغّل السورس من Termux بالأمر:

```bash
cd ~/almutazil-userbot
python -m zlzl
```

قد يوقف Android عملية Termux عند إغلاق التطبيق أو بسبب توفير البطارية؛ لا يُعد التشغيل المحلي بديلاً عن خادم مستمر.
